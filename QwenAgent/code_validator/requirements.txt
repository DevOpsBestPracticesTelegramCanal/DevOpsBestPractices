# Code Validator Dependencies
# ==========================

# Обязательные (core)
# -------------------
# Нет — используется только стандартная библиотека Python 3.10+

# Рекомендуемые (static analysis)
# -------------------------------
ruff>=0.4.0              # Быстрый линтер и форматтер
mypy>=1.10.0             # Проверка типов
bandit>=1.7.0            # Анализ безопасности

# Опциональные (advanced features)
# --------------------------------
hypothesis>=6.100.0      # Property-based тестирование
RestrictedPython>=7.0    # Лёгкая песочница
psutil>=5.9.0            # Мониторинг ресурсов (расширенный)

# Docker (для полной изоляции)
# ----------------------------
# docker>=7.0.0          # Python SDK для Docker (если используете DockerSandbox)

# Разработка и тестирование
# -------------------------
pytest>=8.0.0            # Запуск тестов
pytest-cov>=5.0.0        # Покрытие кода

# Установка:
# ----------
# Минимальная:     pip install ruff mypy bandit
# Полная:          pip install -r requirements.txt
# Только core:     (ничего не нужно, работает из коробки)
