# Core dependencies
requests>=2.28.0
beautifulsoup4>=4.11.0
html2text>=2020.1.16

# Optional: SearXNG (no extra deps needed, uses requests)
# Optional: Ollama (uses requests)

# Optional: Cross-Architecture Review via Claude Haiku
anthropic>=0.39.0

# Testing
pytest>=7.0.0
